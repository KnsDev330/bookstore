import Register from "../components/auth/Resgiter";
import Layout from "./Layout";

const ResgisterPage = () => {
   return (
      <Layout>
         <Register />
      </Layout>
   );
};

export default ResgisterPage;